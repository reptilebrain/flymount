## v1.1.1 - 2026-02-23

### Changed
- Improved CI portability by invoking `flymount.sh` via `bash` in test scripts.
- Installed `sshfs` in CI before running the test suite.
- Added manual CI trigger support (`workflow_dispatch`) to run CI on demand.
- Updated repository docs for private/public badge strategy and release hygiene.
- Completed `master` -> `main` migration references in docs/workflow.

### Added
- CI workflow under `.github/workflows/ci.yml`.
- Public release helper docs:
  - `release-notes/v1.1.0-addendum.md`
  - `release-notes/public-go-live-checklist.md`

### Validation
- `shellcheck` passes for scripts and tests.
- Local test suite passes:
  - `tests/test_parser.sh`
  - `tests/test_targets_validation.sh`
  - `tests/test_install.sh`
  - `tests/test_uninstall.sh`
  - `tests/test_umount_modes.sh`

